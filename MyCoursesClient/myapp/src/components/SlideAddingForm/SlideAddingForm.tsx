import * as React from "react";

export interface ISlideAddingFormProps{

}

export const SlideAddingForm: React.FunctionComponent<ISlideAddingFormProps> = (props:ISlideAddingFormProps)=>{
    return (
        <div>
            123
        </div>
    )
};